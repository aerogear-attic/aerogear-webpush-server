package org.jboss.aerogear.webpush;

import org.junit.Test;

public class WebPushClientIntegrationTest {
    
    @Test
    public void connect() throws InterruptedException {
        /*
        final String documentId = "1234";
        final String clientId = "client2";
        final String originalVersion = "{\"id\": 9999}";
        
        final WebPushClient<String> client = WebPushClient.<String>forHost("localhost").port(7777).path("/sync").build();
        client.connect();
        client.addDocument(clientDoc(documentId, clientId, originalVersion));
        Thread.sleep(1000);
        client.disconnect();
        */
    }
}